# Example only. Put real values into Timeweb environment, never commit them.
NODE_ENV=production
HOST=0.0.0.0
PORT=8080
MCP_AUTH_MODE=secret_path
MCP_PUBLIC_ORIGIN=https://rtalyutin-tg-mcp-fb9b.twc1.net
MCP_PATH_SECRET=REPLACE_WITH_64_HEX_FROM_SECRET_GENERATE
MCP_PROFILE=readonly
PUBLISH_ENABLED=false
# Only for separately approved publisher activation:
# MCP_PROFILE=publisher
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHANNEL_ID=
# PUBLISH_ENABLED=true
