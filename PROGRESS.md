# Продвижение автопубликации сказок

## 19.09.2026 — readiness/lifecycle, пакет 0.7.0

ACTIVE_CONTRACT: реализовать следующий согласованный пункт на базе main
78019b6bedde9749e4a66c7cf9cb73aa15845ca0 и ТЗ 0.19: связать проверку готовности
с интеграционным профилем, проверить сброс, конкуренцию и управляемую остановку.
Разрешены локальные mock, код/GitHub и обновление ТЗ; без внешних Telegram,
Timeweb, платных ресурсов, изменения генератора или включения публикации.

TASK_STATE: startLocalCheckedPublisher запускает три MCP-инструмента, один
Publisher/sender и реальную последовательность getMe/getChat/getChatMember
против обязательного loopback mock. Ручной telegramReady запрещён в этом профиле;
прежний статический harness сохранён как фикстура. Статус обновляется по запросу,
положительного кэша/фонового таймера нет, одновременные проверки объединяются.
Публикационная попытка владеет блокировкой и RAM-записью до ожидания preflight;
повтор читает IN_PROGRESS, другой материал получает BUSY. Ошибка readiness
сохраняется как REJECTED. Ошибка отправки сбрасывает снимок; старый ответ после
сброса/stop не возвращает готовность. Чтение попыток и healthcheck без Telegram.

Добавлены Publisher.stop, идемпотентный close с ожиданием обработки, helper
SIGTERM/SIGINT. Stop не начинает новых частей и не отзывает текущий send;
после ответа сохраняет подтверждение или UNKNOWN. Чтение/дедупликация работают
до закрытия HTTP; при частичной остановке — PARTIAL/SHUTTING_DOWN, до отправки —
REJECTED, полное подтверждение — PUBLISHED. Порядок, точный текст и отсутствие
повторов сохранены. RAM не переживает рестарт; SIGKILL не считается чистой остановкой.

Локальный gate: 89/89 PASS, TypeScript/build PASS, включая 10 независимых
held-out групп. Три built-JS проверки в отдельных процессах PASS: SIGTERM
сохраняет подтверждённый ID и PARTIAL, SIGINT/timeout сохраняет UNKNOWN без
повтора, read-only main по-прежнему не отправляет. Evidence —
QA-LIFECYCLE-2026-09-19.md и verification/2026-09-19-lifecycle/.

Read-only main и ограничения внешнего доступа сохранены. Это не production
entrypoint. OAuth/G1/G3 и реальный grace period Timeweb не проверены. Q2 открыт.
operation_status внешних операций: NOT_STARTED. Действия владельца не нужны.
Следующий пункт: стандартный OAuth 2.1 и production composition root, сначала
локальная проверка с mock авторизации; разработчик сам исследует совместимый
готовый механизм. Не требовать скриншот и не придумывать готовое подключение.
action_decision: CONTINUE. primary_trace: Handoff.

## 19.09.2026 — исправлено необоснованное требование скриншота

По замечанию владельца техническое исследование авторизации остаётся задачей
разработчика. Повторно прочитаны официальные Authentication и Scheduled tasks:
защищённый MCP использует OAuth 2.1; веб-задачи могут пользоваться доступными
чату подключёнными инструментами. Скриншот настроек не нужен для подготовки.
INTEGRATION-GATE.md и TIMEWEB-NATIVE.md исправлены; ТЗ 0.19 уточняет G2.

Это коррекция документации, не реализация OAuth и не доказательство доступа
конкретной задачи. Исторические требования скриншота ниже отменены этой записью.
Код 0.6.0 и прежняя локальная приёмка 73/73 не изменены; тесты заново не запускались.
Следующий шаг сохраняется: readiness/lifecycle на mock и подготовка стандартной
авторизации. Провайдер не выбран. Внешний вход/ресурс/деплой/Telegram не выполнены.
Действия владельца сейчас не нужны. G1/G2/G3 открыты. action_decision: CONTINUE.

## 19.09.2026 — нативная сборка и передача публикации, пакет 0.6.0

ACTIVE_CONTRACT: продолжение main `7664872a36b31768c9c6cd0862a14993f1b87919`.
Владелец согласовал Timeweb App Platform без собственного Dockerfile и поручил
подготовить передачу публикации инструменту. Разрешены код/GitHub, локальная
проверка и подготовка инструкций. Timeweb/Telegram не вызывать.

TASK_STATE: добавлен TelegramReadinessChecker: getMe → getChat → getChatMember,
точный ID/type канала, тот же бот, администратор и право can_post_messages.
Один общий deadline, ограниченный ответ, запрет redirect, без повторов, отправок,
кэша положительного результата и передачи тела ошибки наружу. Это отдельный
read-only модуль; readiness в прежнем loopback harness остаётся тестовым входом.
Production wiring, freshness и lifecycle не реализованы.

Добавлены tsconfig.build.json, build/start:built: только runtime src → dist,
перепись импортов .ts → .js. Скомпилированный main остаётся локальным read-only
probe с прежними ограничениями, не production entrypoint. TIMEWEB-NATIVE.md
описывает подготовленную сборку и оставшиеся настройки по официальным документам
Timeweb. Платформа управляет контейнером сама; собственный Dockerfile не нужен.

PUBLISHING-HANDOFF.md — конкретный блок для будущего добавления к действующей
задаче: один полный текст → один publish_story, чтение попытки при неизвестном
результате, без повторной отправки, Q2 отдельно. Актуальные инструкции генератора
прочитаны, частный prompt в публичный репозиторий не включён. Сам генератор и
расписание не изменены. Перенос ответственности подготовлен, но не активирован.

Проверки: 73/73 PASS (включая 7 новых независимых групп), TypeScript/build PASS.
Независимые 7 групп также прошли на скомпилированном dist/telegram.js. Проверены
запуск готового JS, auth/read-only status, запреты production/внешнего HOST/записи,
полная Unicode-сказка через built MCP и UNKNOWN без повторов. Evidence —
QA-NATIVE-2026-09-19.md и verification/2026-09-19-native/. Локальная приёмка
не подтверждает работоспособность внешнего сервера или фоновой задачи.

Реальные Telegram, Timeweb, OAuth и фоновый MCP-доступ не проверялись.
G1/G2/G3 и Q2 открыты. operation_status деплоя/отправок: NOT_STARTED.
Следующий независимый шаг: соединить проверку readiness с интеграционным
профилем, проверить обновление/сброс готовности, конкурентные вызовы и остановку
процесса на mock. Production auth/публичный entrypoint зависит от G2.
Одно действие владельца: показать экран доступных способов авторизации приватного
MCP в ChatGPT без секретов. Затем выбрать совместимый механизм и завершить
production-профиль; внешний деплой и реальные отправки — отдельный этап.
action_decision: CONTINUE независимую подготовку; активация BLOCKED на G1–G3.

## 19.09.2026 — шаг 5, MCP-интеграция ядра и sender (0.5.0)

ACTIVE_CONTRACT: продолжение ТЗ 0.16 и main
`3f190255d37f784c39e69ef282d225d0d3698ac4`. Соединить один Publisher и
TelegramSender с `publish_story`/`get_publish_attempt`, не меняя безопасный
read-only профиль. Разрешены локальный mock, тесты и GitHub; Timeweb и
Telegram не вызывать, задачу генерации не менять.

TASK_STATE: добавлены общий loopback MCP transport и отдельный integration
harness. Он создаёт один Publisher/TelegramSender на процесс и регистрирует
`get_publisher_status`, `publish_story`, `get_publish_attempt` со строгими
схемами и честными annotations. RAM-состояние и instance_id сохраняются между
MCP-запросами и отдельными клиентскими соединениями. Полный текст проходит
через production formatter и mock HTTP sender.

Прежний `startLocalProbe` остаётся отдельным read-only профилем с одним
инструментом. `src/main.ts` по-прежнему запускает только его и блокирует
production, внешний HOST и PUBLISH_ENABLED. Write-harness принимает только
loopback mock Telegram root и синхронно отвергает официальный/внешний адрес;
его нельзя использовать как production entrypoint.

Локальный gate: 61/61 PASS, TypeScript PASS. Из них 3 новые авторские и
4 независимые held-out MCP-группы. Проверены IN_PROGRESS/BUSY между разными
HTTP-соединениями, полный последовательный текст, PARTIAL на 429, UNKNOWN на
500, отсутствие третьей части и повторов, strict/auth/stale и запрет внешнего
root. Evidence: `test/mcp-*.test.ts`, `QA-MCP-2026-09-19.md`,
`APPSEC-MCP-2026-09-19.md`, `verification/`.

Ограниченная AppSec-проверка: фиксированный канал, Bearer на всех MCP-вызовах,
loopback/Host/Origin, отсутствие секретов в ответах, честные tool annotations,
0 известных production dependency vulnerabilities. Это только локальный
scope; OAuth, proxy/TLS/DNS и реальные права бота не проверены.

Реальные Telegram, Timeweb, OAuth и фоновый MCP-доступ не проверялись.
`telegram_ready` в harness — тестовый вход, а не результат getChat/getChatMember.
G1–G3 открыты, Q2 открыт. operation_status деплоя/отправок: NOT_STARTED.

Следующий конкретный шаг: Dockerfile, production-конфигурация и инструкция
Timeweb без деплоя/платного ресурса/отправок. Затем — фактический способ OAuth
и отдельно разрешённый G1/G3-тест. Действий владельца для подготовки Docker
пока не требуется. action_decision: CONTINUE.

## 18.09.2026 — шаг 4, Telegram HTTP-адаптер (0.4.0)

ACTIVE_CONTRACT: продолжение ТЗ 0.15 и main
`1cefab113fe5de5ff42274b7f8be5f75220caacd`. Реализовать сетевой sender
и проверить его только на локальном HTTP mock. Не обращаться к Telegram,
не развёртывать Timeweb, не менять задачу генерации или её расписание.

TASK_STATE: добавлен `src/telegram.ts`. Один вызов sender создаёт ровно один
POST `sendMessage` с `chat_id` и неизменённым `text`; `parse_mode`,
редиректы и прикладные повторы отсутствуют. Production host закреплён за
HTTPS `api.telegram.org`; loopback HTTP доступен только как тестовый seam.
Ответ ограничен 64 KiB, таймаут 15 с по умолчанию (допустимо 1–60000 мс).

Подтверждение: только HTTP 200 + `ok=true` + положительный безопасный
`message_id`. 429 → RATE_LIMITED, 401/403 → BOT_FORBIDDEN, прочие явные
4xx кроме 408 → SEND_REJECTED. Таймаут заголовков/тела, socket reset, 408,
5xx, редирект, невалидный UTF-8/JSON и неоднозначный/слишком большой ответ
→ UNKNOWN. После UNKNOWN Publisher останавливает оставшиеся части; повторы
attempt/story не создают HTTP-запросов.

Локальный gate: 54/54 групп PASS, TypeScript PASS. Новые проверки:
6 авторских и 9 независимых HTTP-групп. Независимая приёмка выявила два
дефекта авторских проверок: мутирующую границу матрицы и принятие HTTP 201.
Счётчик зафиксирован, подтверждение сужено до HTTP 200; текущие байты
перепроверены. Evidence: `test/telegram*.test.ts`,
`QA-TELEGRAM-2026-09-18.md`, `APPSEC-TELEGRAM-2026-09-18.md`,
`verification/`. Зависимости не добавлены; реальных токенов нет.

Ограниченная AppSec-проверка: редиректы/произвольный production-host
запрещены, токен не входит в JSON/результат/логи, поток ответа ограничен.
Остаток: Bot API помещает токен в URL, поэтому будущие proxy/APM-журналы
должны скрывать исходящий URL; `apiRoot` не выставлять во внешний config.

Реальные Telegram/TLS/DNS/права канала, MCP-инструменты записи, OAuth,
Timeweb и фоновые вызовы не проверены. G1–G3 открыты, Q2 открыт.
operation_status деплоя/отправок: NOT_STARTED.

Следующий конкретный шаг: подключить один Publisher и TelegramSender к MCP,
добавить строгие `publish_story`/`get_publish_attempt`, сохранив отдельный
безопасный read-only профиль. Затем Docker/инструкция Timeweb и G1/G2.
Действий владельца для следующего локального шага не требуется.
action_decision: CONTINUE.

## 17.09.2026 — шаг 3, разбиение полного текста (0.3.0)

ACTIVE_CONTRACT: ТЗ 0.14, разделы 5 и 11; продолжение актуальной main
48c57b98afc98243bc043e36bf09b3cd3e34ad9e. Полный текст последовательными
постами, без постоянного состояния и повторов при неизвестном исходе.
Разрешены разработка, локальная проверка и сохранение кода в GitHub;
Timeweb-деплой и Telegram-публикация не выполняются.

TASK_STATE: добавлен src/formatter.ts и подключён к Publisher по умолчанию.
Границы: абзац → предложение → слово → расширенная графема Intl.Segmenter.
Разделители сохраняются; конкатенация точно равна входу, нет подписей/номеров.
Проверяется выполнимость оставшейся части, чтобы предпочтительный разрез не
создал пробельный пост. Невозможное разбиение и графема сверх бюджета
отклоняются до первого send. Инъекция форматтера также не может разорвать
графему. Порядок, UNKNOWN/PARTIAL, дедупликация и ноль повторов проверены
с настоящим разбиением и mock sender.

Локальный gate: 39/39 групп PASS, TypeScript PASS. Из 15 новых групп —
7 авторских и 8 независимых. Независимый oracle: 32 760 коротких комбинаций
с разными лимитами, 1000 seeded Unicode cases и граница 256 KiB.
Evidence: test/formatter*.test.ts, QA-FORMATTER-2026-09-17.md,
verification/test.tap, verification/typecheck.txt, verification/source-sha256.txt.
Предыдущие логи сохранены в verification/2026-09-16/.
Среда: Node 24.19.0, ICU 78.3, Unicode 17.0; TypeScript target ES2024
для String.isWellFormed. Версии зависимостей не изменены.

Предел 4096 UTF-16 единиц консервативен; emoji могут давать более ранний
разрез. Это не проверка фактического подсчёта/отображения Telegram.
Источник ограничения: https://core.telegram.org/bots/api#sendmessage,
проверен 17.09.2026. G1/G2/G3 не закрыты, Q2 открыт. MCP probe по-прежнему
только читает состояние и с ядром не соединён; сетевой sender отсутствует.

Следующий конкретный шаг: Telegram HTTP-адаптер без повторов и его mock HTTP
проверки (429/5xx, обрыв, таймаут, невалидный ответ, подтверждённый ID).
После этого — MCP-интеграция и Docker/инструкция Timeweb. Внешний блокер:
способ авторизации/подключения, затем отдельное разрешение на деплой/тест.
Для следующего независимого шага действий владельца не требуется.
action_decision: CONTINUE. operation_status деплоя/отправок: NOT_STARTED.

## 16.09.2026 — перенос завершён и проверен

Пакет опубликован в main, коммит 61691ecb632408f90bc94053fe57de80ed8254b5.
Независимый verifier прочитал через GitHub все 31 файл: UTF-8 байты и Git blob
SHA каждого совпали с подготовленным пакетом. Дерево коммита полное
(truncated=false): 31 файл, пропущенных и лишних нет. Это проверка переноса;
тесты повторно не запускались, исходники не менялись. Прежние 24/24 PASS
сохранены в verification/. Текущая запись обновляет только журнал.

Ежедневная задача направлена на актуальную main и этот журнал. Старый ZIP
не использовать для продолжения. ТЗ 0.14 сохранено в исходном документе.
Следующий шаг: Unicode/абзацное разбиение полного текста по разделу 5.
Timeweb-деплой, OAuth, фоновый вызов и Telegram-доставка не выполнялись.

## 16.09.2026 — разрешено публичное размещение

Владелец явно разрешил опубликовать подготовленный пакет в публичный
rtalyutin/Tg-mcp; перед запуском он планирует закрыть репозиторий.
Предыдущий блокер подтверждения снят. Пакет 0.2.0 переносится в корень
ветки main: 31 файл, без node_modules, .env и секретов.
После успешного переноса GitHub — основное место кода, проверок и журнала;
ZIP остаётся историческим снимком. Исходный документ ТЗ сохраняет идентичность.

Исходники и тесты при переносе не менялись; прежняя приёмка 24/24 PASS
и TypeScript PASS относится к этому коду. Деплой и публикации в Telegram
не выполнялись. Следующий шаг: Unicode/абзацное разбиение полного текста.

## 16.09.2026 — выбран GitHub, перенос ожидает подтверждения

Владелец указал https://github.com/rtalyutin/Tg-mcp. Проверено через GitHub:
репозиторий публичный и пустой, подключённому аккаунту доступна запись.
Подготовлены 31 файл пакета 0.2.0 для корня репозитория: исходники, lockfile,
схемы, тесты, README, журнал, отчёты QA и результаты локальных проверок.

Попытка создать README отклонена автоматической проверкой до записи.
Причина: публикация непубличных деталей проекта в публичном репозитории
требует явного подтверждения владельца. Коммитов и файлов не создано;
повторять запись или обходить отклонение без такого подтверждения нельзя.

Необходимое действие владельца: подтвердить публичное размещение исходников,
тестов, README и отчётов из подготовленного пакета в rtalyutin/Tg-mcp.
До этого код и журнал сохраняются в прежнем пакете. После успешного переноса
репозиторий станет основным местом разработки; дублировать исходники в ZIP
после перехода не нужно. Исходный документ ТЗ сохраняет свою идентичность.

Код не изменён: evidence 24/24 тестов и TypeScript PASS сохранён.
Пакет исключает node_modules и .env. Проверка распространённых шаблонов
секретов не нашла совпадений; она не является полной проверкой безопасности.
На Timeweb ничего не развёрнуто, Telegram-отправок не было.

Следующий независимый шаг: Unicode/абзацное разбиение по разделу 5 ТЗ
без потери текста. Оно не требует публикации кода или внешней интеграции.
G1/G2 и Q2 остаются открытыми. action_decision: CONTINUE локально.

## 16.09.2026 — шаг 2, независимое ядро попыток

ACTIVE_CONTRACT: продолжение ТЗ 0.11; локальный код и mock-проверки, без
развёртывания и внешних сообщений. Уточнение от 15.09 сохранено:
«подготовлен код» не означает «на Timeweb работает сервер».

TASK_STATE: пакет 0.2.0; добавлен `src/publisher.ts`, экспорт схем и тесты.
HTTP probe остаётся 0.1.0, к ядру не подключён; MCP-запись не доступна.
Настоящий сетевой Telegram sender ещё не реализован.

Выполнено и проверено:

- RAM-реестр, дедупликация attempt_id/story_id с возвратом исходного ID,
  конфликты содержания и блокировка BUSY без очереди.
- PUBLISHED/REJECTED/PARTIAL/UNKNOWN/IN_PROGRESS; неизвестное состояние
  при смене instance_id или отсутствии записи, без новой отправки.
- Последовательные mock-вызовы, остановка при отказе/неизвестности, отсутствие
  повторов. Replay сохраняет успех после отключения или заполнения реестра.
- Строгая входная схема, валидация полного пакета до первого mock-вызова,
  изоляция возвращаемых объектов от внутренних записей.
- 8 новых авторских групп, 9 новых независимых; с прежними проверками
  общий прогон 24/24 PASS, TypeScript check PASS.
- QA нашёл смену sender.send во время пакета и исключение при разборе
  ответа после вызова sender. Исправления: фиксированный bound send и
  общий try/catch отправки/разбора с terminal UNKNOWN. Независимый повтор PASS.
- Предел 1000 записей проверен на 3000 mock-вызовах для 1000 трёхчастных
  публикаций; переполнение не отправляет, старые записи не вытесняются.

Evidence: QA-CORE-2026-09-16.md, test/publisher-independent.test.ts,
verification/test.tap, verification/typecheck.txt, verification/source-sha256.txt,
verification/core-capacity.json. Это локальная приёмка ядра, не G1–G3.

Production-форматтер ещё не написан: длинный вход отклоняется явным
FORMATTER_NOT_IMPLEMENTED. Частичная доставка проверена на внутренней
фикстуре, не выдаётся за готовое разбиение сказки. Выбор последовательных
постов остаётся согласованным. При будущей MCP-интеграции нужны один
Publisher на процесс, единый instance_id и преобразование ошибок схемы.

Следующий запуск: реализовать Unicode/абзацный форматтер по разделу 5,
проверить точное восстановление текста, лимиты и неделимые символы.
Далее — соединение ядра с MCP и настоящий Telegram-адаптер, без обхода G1/G2.

Блокер внешней интеграции прежний: способ подключения/авторизация в аккаунте,
endpoint и отдельное разрешение на деплой/тестовую отправку. Нового действия
владельца для форматтера не требуется. Q2 не закрыт.

action_decision: CONTINUE. Внешние G1/G2: BLOCKED.
operation_status внешних действий: NOT_STARTED. Публикаций не было.

## 15.09.2026 — шаг 1, локальный транспорт и проверка оснований

ACTIVE_CONTRACT: ТЗ 0.10; следующий конкретный доступный шаг разработки.
Сохранены: Timeweb App Platform, без n8n/БД/постоянного прикладного хранилища,
полные длинные сказки последовательными постами, запрет автоматического
повтора после неизвестного результата. Генератор и его расписание не менять.

TASK_STATE: подготовлен локальный read-only MCP 0.1.0; публикация отсутствует.
Документация подтверждает общий механизм фоновых вызовов подключённых
инструментов; конкретный G1/G2 ещё не пройден.

Выполнено:

- Прочитано актуальное ТЗ (полностью, 315 строк) и файл преемственности.
  В доступных предыдущих результатах код реализации отсутствовал.
- Проверены официальные страницы о задачах, MCP и авторизации;
  выводы и план сквозного теста сохранены в INTEGRATION-GATE.md.
- Реализованы локальный сервер, строгие схемы статуса, тестовый доступ,
  границы HTTP и стабильный instance_id в RAM.
- Авторские проверки: три группы тестов passed; TypeScript check passed.
  Обнаруженные при разработке дефекты настройки типов и теста Host исправлены.
- npm audit --omit=dev: 0 известных уязвимостей по ответу npm на дату запуска;
  это не полная проверка безопасности.

Независимая приёмка: 4 группы PASS; общий итог — 7/7 групп тестов PASS,
TypeScript check PASS. QA нашёл пустой JSON-RPC batch `[]`, ошибочно
принимавшийся с HTTP 202. Исправлено до SDK-dispatch; повтор и регрессия PASS.
Локальный gate — VERIFIED/PASS. См. QA-INDEPENDENT.md и verification/.
Эти наблюдения относятся только к названной локальной версии, не к G1–G3.

Не выполнено: OAuth, подключение в ChatGPT, Timeweb/Docker, Telegram-адаптер,
отправка, разбиение текста, реальная интеграционная приёмка. Ни одной внешней
публикации, платного ресурса или изменения задачи генерации не сделано.

Блокер только внешней интеграции: фактический способ MCP-подключения в
аккаунте и согласование авторизации. Следующее действие владельца — экран
доступных способов подключения приватного MCP без секретов. Ядро не ждёт его.

Следующий запуск: восстановить этот пакет и актуальное ТЗ; реализовать
независимое ядро попыток с mock Telegram по разделу 7: состояния, дедупликация
в RAM, stale-instance, BUSY, частичная/неизвестная отправка без повторов.
Не начинать весь план заново. Не переносить local-probe Bearer на Timeweb.
Q2 по иллюстрации остаётся открыт, текстовое правило уже согласовано.

action_decision: CONTINUE — доступно независимое ядро.
G1/G2: BLOCKED — нет реального подключения и фонового evidence.
operation_status внешних действий: NOT_STARTED.
